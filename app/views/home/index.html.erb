<div class="section no-pad-bot" id="index-banner">
  <div class="container">
    <br><br>
    <div class="row">
      <% if user_signed_in? %>
        <%= render 'dashboard' %>
      <% else %>
        <%= render 'landing' %>
      <% end %>
    </div>
    <br><br>

  </div>
</div>
